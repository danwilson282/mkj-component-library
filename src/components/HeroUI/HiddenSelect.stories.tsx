import { HiddenSelect } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof HiddenSelect> = {
  title: "HeroUI/HiddenSelect",
  component: HiddenSelect,
};

export default meta;

export const Default = {
  args: {},
};
