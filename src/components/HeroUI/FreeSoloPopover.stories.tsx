import { FreeSoloPopover } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof FreeSoloPopover> = {
  title: "HeroUI/FreeSoloPopover",
  component: FreeSoloPopover,
};

export default meta;

export const Default = {
  args: {},
};
