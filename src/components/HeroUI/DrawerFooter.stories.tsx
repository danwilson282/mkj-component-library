import { DrawerFooter } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DrawerFooter> = {
  title: "HeroUI/DrawerFooter",
  component: DrawerFooter,
};

export default meta;

export const Default = {
  args: {},
};
