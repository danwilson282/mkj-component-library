import { DropdownMenu } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DropdownMenu> = {
  title: "HeroUI/DropdownMenu",
  component: DropdownMenu,
};

export default meta;

export const Default = {
  args: {},
};
