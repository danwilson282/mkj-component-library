import { NavbarItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarItem> = {
  title: "HeroUI/NavbarItem",
  component: NavbarItem,
};

export default meta;

export const Default = {
  args: {},
};
