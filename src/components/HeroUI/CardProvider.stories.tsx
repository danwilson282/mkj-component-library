import { CardProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CardProvider> = {
  title: "HeroUI/CardProvider",
  component: CardProvider,
};

export default meta;

export const Default = {
  args: {},
};
