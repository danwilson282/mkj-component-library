import { ListboxSection } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ListboxSection> = {
  title: "HeroUI/ListboxSection",
  component: ListboxSection,
};

export default meta;

export const Default = {
  args: {},
};
