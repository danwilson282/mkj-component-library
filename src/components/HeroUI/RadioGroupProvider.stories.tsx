import { RadioGroupProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof RadioGroupProvider> = {
  title: "HeroUI/RadioGroupProvider",
  component: RadioGroupProvider,
};

export default meta;

export const Default = {
  args: {},
};
