import { ModalFooter } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ModalFooter> = {
  title: "HeroUI/ModalFooter",
  component: ModalFooter,
};

export default meta;

export const Default = {
  args: {},
};
