import { NavbarMenuToggle } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarMenuToggle> = {
  title: "HeroUI/NavbarMenuToggle",
  component: NavbarMenuToggle,
};

export default meta;

export const Default = {
  args: {},
};
