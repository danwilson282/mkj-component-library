import { FormContext } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof FormContext> = {
  title: "HeroUI/FormContext",
  component: FormContext,
};

export default meta;

export const Default = {
  args: {},
};
