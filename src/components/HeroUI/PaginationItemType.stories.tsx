import { PaginationItemType } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof PaginationItemType> = {
  title: "HeroUI/PaginationItemType",
  component: PaginationItemType,
};

export default meta;

export const Default = {
  args: {},
};
