import { NavbarMenuItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarMenuItem> = {
  title: "HeroUI/NavbarMenuItem",
  component: NavbarMenuItem,
};

export default meta;

export const Default = {
  args: {},
};
