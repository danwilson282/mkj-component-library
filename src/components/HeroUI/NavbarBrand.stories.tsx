import { NavbarBrand } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarBrand> = {
  title: "HeroUI/NavbarBrand",
  component: NavbarBrand,
};

export default meta;

export const Default = {
  args: {},
};
