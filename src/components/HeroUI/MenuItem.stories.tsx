import { MenuItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof MenuItem> = {
  title: "HeroUI/MenuItem",
  component: MenuItem,
};

export default meta;

export const Default = {
  args: {},
};
