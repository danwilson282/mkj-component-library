import { ModalProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ModalProvider> = {
  title: "HeroUI/ModalProvider",
  component: ModalProvider,
};

export default meta;

export const Default = {
  args: {},
};
