import { VisuallyHidden } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof VisuallyHidden> = {
  title: "HeroUI/VisuallyHidden",
  component: VisuallyHidden,
};

export default meta;

export const Default = {
  args: {},
};
