import { SelectSection } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof SelectSection> = {
  title: "HeroUI/SelectSection",
  component: SelectSection,
};

export default meta;

export const Default = {
  args: {},
};
