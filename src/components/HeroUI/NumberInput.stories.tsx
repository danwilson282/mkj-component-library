import { NumberInput } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NumberInput> = {
  title: "HeroUI/NumberInput",
  component: NumberInput,
};

export default meta;

export const Default = {
  args: {},
};
