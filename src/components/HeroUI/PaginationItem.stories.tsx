import { PaginationItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof PaginationItem> = {
  title: "HeroUI/PaginationItem",
  component: PaginationItem,
};

export default meta;

export const Default = {
  args: {},
};
