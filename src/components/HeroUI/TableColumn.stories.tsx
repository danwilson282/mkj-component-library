import { TableColumn } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TableColumn> = {
  title: "HeroUI/TableColumn",
  component: TableColumn,
};

export default meta;

export const Default = {
  args: {},
};
