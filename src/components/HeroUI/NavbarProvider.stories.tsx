import { NavbarProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarProvider> = {
  title: "HeroUI/NavbarProvider",
  component: NavbarProvider,
};

export default meta;

export const Default = {
  args: {},
};
