import { DateRangePicker } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DateRangePicker> = {
  title: "HeroUI/DateRangePicker",
  component: DateRangePicker,
};

export default meta;

export const Default = {
  args: {},
};
