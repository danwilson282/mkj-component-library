import { SelectItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof SelectItem> = {
  title: "HeroUI/SelectItem",
  component: SelectItem,
};

export default meta;

export const Default = {
  args: {},
};
