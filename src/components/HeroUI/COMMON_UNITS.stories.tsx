import { COMMON_UNITS } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof COMMON_UNITS> = {
  title: "HeroUI/COMMON_UNITS",
  component: COMMON_UNITS,
};

export default meta;

export const Default = {
  args: {},
};
