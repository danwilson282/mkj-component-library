import { DrawerHeader } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DrawerHeader> = {
  title: "HeroUI/DrawerHeader",
  component: DrawerHeader,
};

export default meta;

export const Default = {
  args: {},
};
