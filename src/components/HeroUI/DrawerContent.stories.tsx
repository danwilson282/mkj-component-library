import { DrawerContent } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DrawerContent> = {
  title: "HeroUI/DrawerContent",
  component: DrawerContent,
};

export default meta;

export const Default = {
  args: {},
};
