import { DropdownTrigger } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DropdownTrigger> = {
  title: "HeroUI/DropdownTrigger",
  component: DropdownTrigger,
};

export default meta;

export const Default = {
  args: {},
};
