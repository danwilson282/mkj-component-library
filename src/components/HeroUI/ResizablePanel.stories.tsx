import { ResizablePanel } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ResizablePanel> = {
  title: "HeroUI/ResizablePanel",
  component: ResizablePanel,
};

export default meta;

export const Default = {
  args: {},
};
