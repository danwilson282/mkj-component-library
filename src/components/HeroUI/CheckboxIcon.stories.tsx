import { CheckboxIcon } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CheckboxIcon> = {
  title: "HeroUI/CheckboxIcon",
  component: CheckboxIcon,
};

export default meta;

export const Default = {
  args: {},
};
