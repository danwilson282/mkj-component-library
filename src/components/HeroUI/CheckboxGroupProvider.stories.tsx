import { CheckboxGroupProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CheckboxGroupProvider> = {
  title: "HeroUI/CheckboxGroupProvider",
  component: CheckboxGroupProvider,
};

export default meta;

export const Default = {
  args: {},
};
