import { NavbarMenu } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarMenu> = {
  title: "HeroUI/NavbarMenu",
  component: NavbarMenu,
};

export default meta;

export const Default = {
  args: {},
};
