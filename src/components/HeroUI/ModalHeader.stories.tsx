import { ModalHeader } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ModalHeader> = {
  title: "HeroUI/ModalHeader",
  component: ModalHeader,
};

export default meta;

export const Default = {
  args: {},
};
