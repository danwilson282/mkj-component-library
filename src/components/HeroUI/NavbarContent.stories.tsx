import { NavbarContent } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof NavbarContent> = {
  title: "HeroUI/NavbarContent",
  component: NavbarContent,
};

export default meta;

export const Default = {
  args: {},
};
