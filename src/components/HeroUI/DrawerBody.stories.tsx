import { DrawerBody } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DrawerBody> = {
  title: "HeroUI/DrawerBody",
  component: DrawerBody,
};

export default meta;

export const Default = {
  args: {},
};
