import { AvatarGroupProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof AvatarGroupProvider> = {
  title: "HeroUI/AvatarGroupProvider",
  component: AvatarGroupProvider,
};

export default meta;

export const Default = {
  args: {},
};
