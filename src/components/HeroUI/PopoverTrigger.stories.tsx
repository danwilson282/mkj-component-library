import { PopoverTrigger } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof PopoverTrigger> = {
  title: "HeroUI/PopoverTrigger",
  component: PopoverTrigger,
};

export default meta;

export const Default = {
  args: {},
};
