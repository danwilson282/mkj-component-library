import { PaginationCursor } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof PaginationCursor> = {
  title: "HeroUI/PaginationCursor",
  component: PaginationCursor,
};

export default meta;

export const Default = {
  args: {},
};
