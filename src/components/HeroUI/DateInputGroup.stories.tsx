import { DateInputGroup } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DateInputGroup> = {
  title: "HeroUI/DateInputGroup",
  component: DateInputGroup,
};

export default meta;

export const Default = {
  args: {},
};
