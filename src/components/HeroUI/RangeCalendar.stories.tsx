import { RangeCalendar } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof RangeCalendar> = {
  title: "HeroUI/RangeCalendar",
  component: RangeCalendar,
};

export default meta;

export const Default = {
  args: {},
};
