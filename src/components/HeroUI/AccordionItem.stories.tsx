import { AccordionItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof AccordionItem> = {
  title: "HeroUI/AccordionItem",
  component: AccordionItem,
};

export default meta;

export const Default = {
  args: {},
};
