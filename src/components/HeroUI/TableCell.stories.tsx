import { TableCell } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TableCell> = {
  title: "HeroUI/TableCell",
  component: TableCell,
};

export default meta;

export const Default = {
  args: {},
};
