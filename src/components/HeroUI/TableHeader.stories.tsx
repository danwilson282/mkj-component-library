import { TableHeader } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TableHeader> = {
  title: "HeroUI/TableHeader",
  component: TableHeader,
};

export default meta;

export const Default = {
  args: {},
};
