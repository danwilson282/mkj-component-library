import { TimeInput } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TimeInput> = {
  title: "HeroUI/TimeInput",
  component: TimeInput,
};

export default meta;

export const Default = {
  args: {},
};
