import { DateInputSegment } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DateInputSegment> = {
  title: "HeroUI/DateInputSegment",
  component: DateInputSegment,
};

export default meta;

export const Default = {
  args: {},
};
