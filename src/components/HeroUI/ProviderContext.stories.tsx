import { ProviderContext } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ProviderContext> = {
  title: "HeroUI/ProviderContext",
  component: ProviderContext,
};

export default meta;

export const Default = {
  args: {},
};
