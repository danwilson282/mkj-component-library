import { LinkIcon } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof LinkIcon> = {
  title: "HeroUI/LinkIcon",
  component: LinkIcon,
};

export default meta;

export const Default = {
  args: {},
};
