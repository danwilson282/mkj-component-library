import { ModalContent } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ModalContent> = {
  title: "HeroUI/ModalContent",
  component: ModalContent,
};

export default meta;

export const Default = {
  args: {},
};
