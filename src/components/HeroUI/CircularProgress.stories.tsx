import { CircularProgress } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CircularProgress> = {
  title: "HeroUI/CircularProgress",
  component: CircularProgress,
};

export default meta;

export const Default = {
  args: {},
};
