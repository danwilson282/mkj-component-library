import { DateInputField } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof DateInputField> = {
  title: "HeroUI/DateInputField",
  component: DateInputField,
};

export default meta;

export const Default = {
  args: {},
};
