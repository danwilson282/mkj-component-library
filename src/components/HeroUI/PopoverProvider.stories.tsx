import { PopoverProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof PopoverProvider> = {
  title: "HeroUI/PopoverProvider",
  component: PopoverProvider,
};

export default meta;

export const Default = {
  args: {},
};
