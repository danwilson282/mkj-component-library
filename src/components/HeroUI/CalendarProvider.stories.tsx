import { CalendarProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CalendarProvider> = {
  title: "HeroUI/CalendarProvider",
  component: CalendarProvider,
};

export default meta;

export const Default = {
  args: {},
};
