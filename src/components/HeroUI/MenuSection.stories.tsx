import { MenuSection } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof MenuSection> = {
  title: "HeroUI/MenuSection",
  component: MenuSection,
};

export default meta;

export const Default = {
  args: {},
};
