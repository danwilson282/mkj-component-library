import { TableBody } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TableBody> = {
  title: "HeroUI/TableBody",
  component: TableBody,
};

export default meta;

export const Default = {
  args: {},
};
