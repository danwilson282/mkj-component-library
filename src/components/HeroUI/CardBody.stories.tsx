import { CardBody } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CardBody> = {
  title: "HeroUI/CardBody",
  component: CardBody,
};

export default meta;

export const Default = {
  args: {},
};
