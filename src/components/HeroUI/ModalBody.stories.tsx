import { ModalBody } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ModalBody> = {
  title: "HeroUI/ModalBody",
  component: ModalBody,
};

export default meta;

export const Default = {
  args: {},
};
