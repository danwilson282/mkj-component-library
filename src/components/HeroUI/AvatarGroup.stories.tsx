import { AvatarGroup } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof AvatarGroup> = {
  title: "HeroUI/AvatarGroup",
  component: AvatarGroup,
};

export default meta;

export const Default = {
  args: {},
};
