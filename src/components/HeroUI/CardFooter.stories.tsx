import { CardFooter } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CardFooter> = {
  title: "HeroUI/CardFooter",
  component: CardFooter,
};

export default meta;

export const Default = {
  args: {},
};
