import { TableRow } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof TableRow> = {
  title: "HeroUI/TableRow",
  component: TableRow,
};

export default meta;

export const Default = {
  args: {},
};
