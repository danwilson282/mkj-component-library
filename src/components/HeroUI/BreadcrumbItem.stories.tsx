import { BreadcrumbItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof BreadcrumbItem> = {
  title: "HeroUI/BreadcrumbItem",
  component: BreadcrumbItem,
};

export default meta;

export const Default = {
  args: {},
};
