import { CardHeader } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof CardHeader> = {
  title: "HeroUI/CardHeader",
  component: CardHeader,
};

export default meta;

export const Default = {
  args: {},
};
