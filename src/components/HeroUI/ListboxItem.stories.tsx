import { ListboxItem } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ListboxItem> = {
  title: "HeroUI/ListboxItem",
  component: ListboxItem,
};

export default meta;

export const Default = {
  args: {},
};
