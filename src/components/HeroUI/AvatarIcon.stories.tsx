import { AvatarIcon } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof AvatarIcon> = {
  title: "HeroUI/AvatarIcon",
  component: AvatarIcon,
};

export default meta;

export const Default = {
  args: {},
};
