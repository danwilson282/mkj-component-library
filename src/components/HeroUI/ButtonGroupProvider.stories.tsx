import { ButtonGroupProvider } from "@heroui/react";
import { Meta } from "@storybook/react";

const meta: Meta<typeof ButtonGroupProvider> = {
  title: "HeroUI/ButtonGroupProvider",
  component: ButtonGroupProvider,
};

export default meta;

export const Default = {
  args: {},
};
